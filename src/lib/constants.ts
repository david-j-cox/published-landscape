export const PLACEMENT_STORAGE_KEY = "pl:pendingPlacement";

/** The submit page's last query + results, so returning from the map does not
 *  mean re-typing the title and abstract. Survives until the tab closes. */
export const SUBMIT_STATE_KEY = "pl:submitState";
